1. How to handle "lazy" serializer.  We can't use BSONReader/BSONWriter for this, so there needs to be a way around
   that to go straight to the to InputBuffer/OutputBuffer

2. Design a proper exception hierarchy and standard message formats.

3. Final decision on how to instantiate MongoClient, whether MongoClient is an interface, abstract class, or concrete
   class

4. Better design for Serializer.

5. What to do about the GridFS API.  Move it to org.mongodb as well?

6. Configurable retry logic for reads
